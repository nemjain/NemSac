package utility;

public class CaptureScreenShot {

	/*public static String fileName;

	public WebDriver driver;

	public void getScreenShot(WebDriver driver){
		
		this.driver = driver;
		
		Date d = new Date();
		fileName = d.toString().replace(":", "_").replace(" ", "_") + ".jpg";
		

		File screenshot = ((TakesScreenshot) this.driver).getScreenshotAs(OutputType.FILE);
		FileUtils.copyFile(screenshot, new File(System.getProperty("user.home" + "\\git\\NemSac\\nemsac\\") + fileName));
		
		//FileUtils.copyFile(screenshot, new File("./target/failScreenshot" + fileName));
	}
*/
}
